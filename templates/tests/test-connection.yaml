apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "user-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
